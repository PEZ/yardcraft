(ns yardcraft.scratch
  "Optional bootstrap helper — prefer root user.lpy / user/init! after Calva connect.")

(comment
  ;; If needed without user.lpy:
  ;; (import sys)
  ;; (.insert (.-path sys) 0 "src")
  :rcf)
