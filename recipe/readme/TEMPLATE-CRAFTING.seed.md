# Yardcraft — template crafting (seed)

**Audience:** agents (and humans) working on the **Yardcraft recipe / template itself** — not a visitor Hello session for someone’s yard.

This is the **tracked seed**. Copy to repo root as `TEMPLATE-CRAFTING.md` (gitignored living pad). Edit the living copy day-to-day; promote durable orientation back into this seed when the pattern stabilizes.

```
λ craft_gate.
  work ≡ improve(AGENTS ∨ skills ∨ story ∨ code ∨ packaging)
  → prefer(this_file) over(clone_Hello_playbook)
  | work ≡ help_human_yardcraft_their_site
  → AGENTS.md + phase skills (ignore this file)
```

Clone-facing playbook stays in **`AGENTS.md`** + `recipe/skills/`. Do not dump crafting meta into those.

---

## Role

You are pair-programming the **press-release delivery system**: story shows the *kind* of experience; `AGENTS.md` + skills make an agent able to deliver that kind of experience in a clone.

- **Story SoT (experience):** `README.md`, optional `MY-BASE-DESIGN-PROCESS.md`
- **Clone playbook SoT:** `AGENTS.md` (thin gates + living Phase) + skills under `recipe/skills/`
- **This file:** authoring orientation, open recipe work, collaboration rules for PEZ ⊗ agent on the template

## Collaboration (hard)

- PEZ owns story voice and beats in `README.md` / `MY-BASE-DESIGN-PROCESS.md`.
- Agent: **understand** those; only **spelling and grammar** unless asked to draft.
- Unclear / odd / intentional-looking phrasing → **ask**, don’t “fix” (including memoir lines like “more better”).
- Design the experience in the story docs first; then update `AGENTS.md` / skills so a real agent can deliver **that kind of experience** — not a verbatim replay of the example chat.
- **One shape ever** in anything clones read: rewrite the present; past lives stay in git only.
- **Speak so a visitor can understand** remains the bar for all human-facing clone text (see `AGENTS.md`).

## Where to edit what

| Concern | Home |
|---|---|
| Clone Hello / toolchain | `yardcraft-setup` (+ Phase table in `AGENTS.md`) |
| Real base / maps / facts | `yardcraft-base-design` + composables (light-table, country, epupp) |
| Connect / bpy / quirks | `basilisp-blender` (+ `basilisp` dialect) |
| Harness depth budget | `recipe/readme/subagents/harness-model.md` (constraints); live steps in setup skill |
| Packaged skills | `recipe/skills/` — copy into harness (e.g. `.cursor/skills/`) for local trials; `.cursor/` is gitignored |

## Working loop

1. **Observe** current `AGENTS.md`, relevant skills, code, REPLs — don’t assume from chat memory.
2. Prefer **small** recipe changes; verify on `bb` and/or `basilisp-blender` when code/docs claim behavior.
3. Keep clone docs **progressive**: thin gate in `AGENTS.md`, depth in one canonical skill/ref, pointers elsewhere.
4. After meaningful recipe shifts, update **this** file’s Open / Notes so the next crafting session resumes cleanly.

## Open

- Exact layer 1→2 / 2→3 flip moments (demo vs “ready for material” vs explicit “base done”)
- Full narrative fly tour content (panel path guarded empty until authored)
- README quote-plan empty-facts hedge — optional PEZ pen
- Periodic **code sanity** sweeps after story/code churn
- Keep `AGENTS.md` / skills aligned with story after any story edit

## Notes (scratch freely)

- 
